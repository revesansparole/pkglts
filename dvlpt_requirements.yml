# {# pkglts, conda
# requirements are managed by pkglts, do not edit this file at all
# edit .pkglts/pkg_cfg instead
# section pysetup
channels:
- defaults

dependencies:
- coverage
- flake8
- nbconvert
- pytest
- pytest-cov
- pytest-mock
- sphinx
- pip:
  - coveralls
  - tox
  - twine

# #}
