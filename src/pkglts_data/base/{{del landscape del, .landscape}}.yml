TODO, problem with landscape does not like this file!!!!
TODO, hence the name of the file to be sure that nothing is created
TODO, whatever happen

doc-warnings: yes
test-warnings: no
strictness: veryhigh
max-line-length: 120
uses:
    - django
    - celery
autodetect: yes
requirements:
    - deps/core.txt
    - deps/test.txt
ignore-paths:
    - docs
    - mypackage/vendor
ignore-patterns:
    - ^example/doc_.*\.py$
    - (^|/)docs(/|$)
