[distutils]
# {# pkglts, pypi.serverlist
index-servers=
{%- for server in pypi.servers %}
    {{ server['name'] }}
{%- endfor -%}
# #}

# {# pkglts, pypi.serverdescr
{%- for server in pypi.servers -%}
[{{ server['name'] }}]
repository = {{ server['url'] }}

{% endfor -%}
# #}
