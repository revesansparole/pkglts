[distutils]
# {# pkglts, pypi.serverlist
index-servers=
{%- for name in pypi.servers.keys() %}
    {{ name }}
{%- endfor -%}
# #}

# {# pkglts, pypi.serverdescr
{%- for name, descr in pypi.servers.items() -%}
[{{ name }}]
repository = {{ descr['url'] }}

{% endfor -%}
# #}
