# {# pkglts, conda
# requirements are managed by pkglts, do not edit this file at all
# edit .pkglts/pkg_cfg instead
# section pysetup
channels:
- defaults

dependencies:
{%- for dep in pysetup.requirements('dvlpt') if dep.is_conda(strict=False) %}
- {{ dep.fmt_conda_requirement() }}
{%- endfor %}

{%- set pip_deps = pysetup.requirements('dvlpt')|select("is_pip_dep", True)|list -%}
{%- if pip_deps|length > 0 %}
- pip:
{%- for dep in pip_deps %}
  - {{ dep.fmt_conda_requirement() }}
{%- endfor %}
{% endif %}
# #}
